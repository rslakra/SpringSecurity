server.port=8080

# JPA Settings
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=create-drop

# H2 Database Settings (in-memory for tests)
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (disabled for tests)
spring.h2.console.enabled=false

# ========================================
# OAuth2 Authorization Server Configuration
# ========================================

# OAuth Client Configuration
oauth.client.id=test-client
oauth.client.secret={bcrypt}$2a$10$GRLdNijSQMUvl/au9ofL.eDwmoohzzS7.rmNSJZ.0FxO/BTk76klW
oauth.client.redirect-uris=http://localhost:8090/callback
oauth.client.scopes=openid,read,write

# Foo Client Configuration (different secret!)
oauth.foo-client.id=foo-client
oauth.foo-client.secret={bcrypt}$2a$10$N0JqY/D9QS9c9N6F5l0LgO5v2C3RglJXQEZRLBzpEj1u/xU7S6cS2
oauth.foo-client.redirect-uris=http://localhost:8080/callback
oauth.foo-client.scopes=openid,read,write

# Token Settings
oauth.token.access-token-ttl-minutes=3
oauth.token.refresh-token-ttl-minutes=10
